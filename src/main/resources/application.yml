# Spring Data Source ?? - MySql DB ?? ??? ??

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todoappdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: jungwon
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show-sql: true
        format_sql: true
#    database-platform: org.hibernate.spatial.dialect.mysql.MySQL56InnoDBSpatialDialect